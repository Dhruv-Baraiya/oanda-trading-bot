{
  "name": "oanda-trading-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Automated Forex trading bot using OANDA v20 REST API",
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
